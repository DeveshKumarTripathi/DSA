class Solution {
    public int firstMissingPositive(int[] arr) {
        Arrays.sort(arr);
        int num = 1;
        
        for(int i=0; i<arr.length; i++){
            if(arr[i] == num && arr[i] >= 1){
                num++;
            }
        }
        return num;
    }
}
